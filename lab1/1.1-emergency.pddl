(define (domain WORLD)
        (:requirements :strips);; require STRIPS
        (:predicates )

        (:action ACTION_1
        [:parameters ]
        [:precondition ]
        [:effect ]
        )
)